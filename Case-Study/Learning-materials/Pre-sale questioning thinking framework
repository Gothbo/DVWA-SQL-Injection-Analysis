# 售前追问四维框架：从"泛泛而谈"到"精准服务"的分水岭

> 客户描述完痛点后，追问的本质不是收集信息，而是**画出方案边界**——你问的每一条信息，都是为了下一次开口时能给出一张客户信服的准确清单。

---

## 一、环境维度 — 系统架构（决定方案的技术边界）

| 追问项 | 为什么重要 | 影响链路 |
|--------|-----------|---------|
| 线上商城是**自研**还是**第三方SaaS**（如有赞、微盟）？ | SaaS平台你管不了底层，只能做应用层/API级防护；自研才能做深度的WAF调优、主机级防护 | SaaS → 配合平台风控 + 前端JS-SDK；自研 → WAF深度调优 + 主机防护 |
| 服务器在**云上**还是**私有机房**？ | 云上可以直接上云WAF、云原生CC防护；私有机房要单独考虑带宽和硬件部署 | 云上 → 云WAF + 弹性扩容；机房 → 硬件设备 + 带宽规划 |
| 如果是云，是哪家**云厂商**？ | 各家云WAF的规则引擎、日志能力差异大，方案要适配 | 阿里云 → Bot管理 + CC防护；腾讯云 → 大禹高防；AWS → WAF + Shield |
| 业务是**单体架构**还是**微服务**？有无**API网关**？ | 有API网关的话，限流、鉴权等策略可以在网关层做，方案更轻量 | 有网关 → 网关限流 + WAF协同；无网关 → WAF全量接管 |
| **数据库**用的是什么？有无备库？ | 数据篡改类问题需要看数据库审计能力，有备库可以快速回滚 | 有备库 → 溯源 + 回滚方案；无备库 → 重点防篡改 + 备份建议 |

### 实战示例

同样是WAF调优方案，环境不同方案天差地别：

- **客户A**：阿里云WAF + 自营电商 → 在云WAF上直接开启Bot管理模块 + CC防护策略
- **客户B**：某标准SaaS商城 + 无自研能力 → 只能配合平台自带风控，推荐前端防护JS-SDK + 业务层限流

---

## 二、时间维度 — 整改期限（决定方案的节奏和范围）

| 追问项 | 为什么重要 | 影响链路 |
|--------|-----------|---------|
| "一周内"是指**出报告**还是**全部修好**？ | 出报告 → 方案只需聚焦溯源排查；全修好 → 要出完整实施计划 | 报告 → 3天溯源 + 2天输出；修好 → 5-7天完整实施 |
| 有没有硬的**deadline**（大促、等保检查、审计）？ | 有硬deadline的话方案要做取舍：先止血保底线，后治本建能力 | 有硬限 → 分两期：应急加固（1期）+ 能力建设（2期） |
| 是否有**窗口期**可以停服维护？ | 不能停服的话，必须选热部署、灰度切换、旁路部署等方案 | 可停服 → 直接操作；不可停服 → 旁路部署 + WAF热更新 |

### 实战示例

同一客户，时间定义不同方案完全不同：

- **场景A**：一周内要**报告** → 1-2天快速溯源排查 + 2-3天针对性加固 + 输出完整报告
- **场景B**：一周内要**全修好且不能停服** → 零停服WAF热更新 + 旁路态势感知 + 分批次灰度切换

---

## 三、资产维度 — 存量设备（决定方案的兼容性与复用度）

| 追问项 | 为什么重要 | 影响链路 |
|--------|-----------|---------|
| 现有的**WAF**是什么品牌和型号？版本号？ | 决定了调优方案的可行性和日志对接方式的兼容性 | 主流品牌 → 标准化对接；老旧型号 → 可能需要升级或替换 |
| **IPS/IDS**是什么品牌？有无日志导出接口（Syslog/API）？ | 决定能不能做日志统一分析和设备联动 | 有接口 → 日志接入关联分析；无接口 → 旁路流量镜像补齐 |
| 有无**EDR**终端防护？部署覆盖率多少？ | 如果是主机侧攻击，EDR进程级日志是关键证据源 | 有EDR → 主机侧溯源；无EDR → 需要部署轻量Agent或流量分析 |
| 有没有**日志分析/SIEM**类产品？ | 有的话直接复用现有平台，没有的话需要纳入方案成本 | 已有SIEM → 扩展数据源接入；无SIEM → 推荐日志分析一体机 |
| **安全团队**配置如何？专人有安全背景还是IT兼职？ | 决定后续服务模式：专人 → 工具赋能即可；兼职 → 推荐MSS托管运营 | 专人 → 产品+培训；兼职 → MSS托管+主动巡检 |

---

## 四、预算维度 — 成本边界（决定方案的档次与深度）

预算问题要**温和地问**，不要一上来就问"你们有多少钱"。建议在用完前三问、给出方案框架之后，用"套餐选择"的方式自然过渡。

### 推荐的话术模板

> "根据您目前的情况，我们大概有两种方案方向：
>
> **方案A**：在现有设备基础上做策略调优和日志打通，投入大概 **2-3万**，侧重解决眼前溯源问题。
>
> **方案B**：上一套完整的XDR + 态势感知平台，投入大概 **10-15万**，实现长期主动防御和自动化运营。
>
> 您更倾向于哪个方向？或者您心里的预算预期大概在什么范围？我们好帮您做精确配置。"

### 预算范围与方案对应关系

| 预算范围 | 可交付的方案 | 适用场景 |
|---------|-------------|---------|
| 1-3万 | 应急溯源服务 + WAF/IPS策略调优 | 仅解决眼前问题，先出报告 |
| 5-10万 | 轻量XDR + 日志统一分析 + 短期运维 | 解决溯源 + 建立基础感知能力 |
| 15万+ | 全套XDR+SIP + MSS托管运营服务 | 长期安全运营能力建设 |

> **关键原则**：用"方案选择"替代"预算试探"。客户选择方案A还是方案B，比直接回答"预算多少"心理负担小得多，但你能得到的信息量是一样的。

---

## 五、追问时机与顺序（完整节奏）

新手售前最常见的错误不是问题没问到，而是**顺序问反了**。正确的五步节奏：

```
痛点翻译 ──→ 共情锚定 ──→ 环境维度 ──→ 时间维度 ──→ 资产维度 ──→ 方案框架 ──→ 预算维度
   ↑            ↑            ↑            ↑            ↑            ↑            ↑
 先确认      建立信任     最中立       客户愿意    顺理成章    给出2-3档    用套餐选择
 痛点无误    降低防御     最容易回答   分享排期    引出设备     方案方向    自然引出
```

### 核心原则

**先问"环境和技术"，再问"时间和预算"。**

- 环境信息（自研/SaaS、云上/机房）—— 客户不敏感、愿意分享，先建立问答节奏
- 时间节点（deadline类型）—— 客户有表达欲，可以顺势深入
- 存量设备（品牌型号）—— 涉及方案可行性，技术负责人愿意配合
- 预算金额 —— 敏感问题，信任建立后再问，用套餐选择过渡

---

## 六、完整话术速查表

| 维度 | 问什么 | 推荐话术 |
|------|--------|---------|
| 环境 | 系统架构 | "您这边线上商城是自研的，还是用的第三方SaaS平台？这个决定了我们防护落地的具体方式。" |
| 环境 | 部署方式 | "服务器是在云上还是自有机房？如果是云上，方便说是哪家云厂商吗？" |
| 环境 | 架构细节 | "业务是单体架构还是微服务？有在用API网关吗？" |
| 时间 | 期限定义 | "您说的一周内，是老板要看到一份报告结果，还是需要把所有问题都修复完？两个方案节奏不一样。" |
| 时间 | 硬性节点 | "最近有没有大促、等保检查或者其他硬性的时间节点需要赶上？" |
| 时间 | 停服窗口 | "到时候做调整的话，有窗口期可以停服维护吗？还是要求业务完全不能中断？" |
| 资产 | WAF/IPS | "方便说一下目前WAF和IPS用的什么品牌和型号吗？我们确认一下日志对接的方式。" |
| 资产 | 安全团队 | "您这边安全团队大概几个人？平时告警是自己看，还是已经有外包在帮忙看？" |
| 资产 | EDR/SIEM | "目前有部署EDR终端防护或者日志分析平台吗？覆盖率怎么样？" |
| 预算 | 预算范围 | "根据您的情况大概有两种方向，一种是基于现有设备优化投入2-3万，另一种是上完整平台10-15万。您倾向哪个方向？" |

---

## 七、一句话总结

> **售前追问不是盘问客户，而是帮客户画出方案边界。**
>
> 问环境，是在画技术边界；
> 问时间，是在画节奏边界；
> 问资产，是在画兼容边界；
> 问预算，是在画成本边界。
>
> 四条边界画清楚了，方案就是填空——而不是猜谜。
